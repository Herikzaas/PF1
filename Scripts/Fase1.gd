extends Node2D


# Called when the node enters the scene tree for the first time.
func _ready():
	pass
	#for i in StaticData.itemData['perguntas'] :
		#if StaticData.itemData['perguntas'][i]['id'] == 1 :
			#print(StaticData.itemData['perguntas'][i]["cost"])

# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta):
	pass
